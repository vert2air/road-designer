# Changelog

このプロジェクトのすべての変更はこのファイルに記録されます。  
フォーマットは [Keep a Changelog](https://keepachangelog.com/ja/1.0.0/) に準拠し、  
バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) に従います。

---

## [1.1.0] - 2026-06-13

### Added

#### 平面線形設計
- **TwoLineOffsetConstraint（2直線+1円のオフセット拘束）**: 円を 2 直線からのオフセット距離で拘束。直線の移動・円の半径変更に円中心が自動追従
- オフセット拘束の循環依存検出: 設定時にグラフ探索（BFS）でループを検出し、警告して登録を拒否
- オフセット拘束の連鎖伝播: TwoLineOffsetConstraint → OffsetConstraint の連鎖追従
- **ラバーバンド選択**: `Shift`+ドラッグの矩形に完全に含まれる図形を一括選択（親図形も同時選択）。ドラッグ中は対角距離を表示する簡易測距ツールを兼ねる
- **複数選択 AABB 操作**: 2 図形以上の選択で AABB 枠線とハンドルを表示。辺=平行移動・頂点=XY 等率拡縮・対角線=回転（スナップショット方式で累積誤差なし、Undo 対応）

#### 右パネル
- 複数選択時の操作パネル: コピー（縦断線形データも複製）・平行移動・回転・拡大縮小（XY 同率）を数値入力で実行
- 関連オフセット拘束の一覧表示と「選択」ボタンによる拘束パネルへの切り替え
- Paste の右クリック変換メニュー: 原点基準の回転（90°/180°/−90°）・線対称（y=0 / x=0 / y=x / y=−x）
- スピンボックスのフォーカス制御: クリックでフォーカスを得た後のみホイールで値が変化（誤操作防止）
- 「ニックネームで選択」エリアの折りたたみ・スプリッターによるリサイズ

#### ファイル操作
- **追加で読み込む（`Ctrl+Shift+O`）**: 現在のシーンに別ファイルをマージ。ID 衝突は自動解決し、追加図形のみ選択状態にする（Undo 対応）
- **ID を振り直す**（メニュー）: 全図形の ID を 1 から連番化。ニックネーム・クロソイド参照も追従
- ニックネームのデフォルト廃止: 未設定図形は `(タイプ#id)` 形式で表示のみ（ファイルには保存しない）

#### 3D走行ビューア
- ステアリングホイールグラフィック（曲率連動の切れ角表示）
- 速度変更の 2 段階化（`↑`/`↓`=±3 m/s、`Shift`+=±10 m/s）

#### テスト・品質
- 単体テスト 1,372件 + 仕様適合テスト 112件（第8章メニュー・ショートカット章を新設）
- 合算カバレッジ 94%（C0 96% / C1 88%）
- 仕様書・基本設計書・詳細設計書・docstring をソース実装と全面突き合わせして最新化

### Fixed

- TwoLineOffsetConstraint: ファイルロード後の初回 solve() が保存済みオフセット値を上書きし、最初の直線移動に円が追従しないバグ
- マージ時に ID 衝突があるとニックネームが無関係な既存図形へ付け替わるバグ
- 線分プロパティの t 値スピンボックスがレイアウトに追加されておらず GUI から入力できないバグ
- オフセット変更時の描画更新遅延とクロソイド端点ズレ
- 円弧・線分の端点が接点由来かどうかを認識できない問題（clothoid ID による追跡へ根本修正）
- road_viewer: ニックネームが None のとき encode() が失敗するバグ
- クロソイド区間でステアリングホイールが瞬間的に戻るバグ
- update_selection で scene 変更後にコンボの findText が失敗する問題
- 縦断線形カラーバーのラベルを 3 行化し、円弧・線分のニックネーム表示を修正

### Changed

- シグナル系統の整理・重複コードの共有ヘルパー集約（リファクタリング）
- `uv run pytest` で HTML カバレッジレポートを自動生成

---

## [1.0.0] - 2026-05-24

### Added

#### 平面線形設計（第4章）
- 直線モード（`L`）: 左クリックで始点→終点を指定して直線・線分を追加
- 円モード（`C`）: ドラッグで中心と半径を指定して円を追加
- 選択モード（`S`）: クリックで図形を選択、`Shift`+クリックで複数選択
- クロソイド曲線: 線分と円弧の間に自動生成（Fresnel 積分、scipy 不使用）
- スムーズ接続: 隣接する線分と円弧をクロソイドで自動接続
- オフセット拘束: 直線を 2 つの円に対してオフセット量で拘束（円の移動に自動追従）
- `Del` キーで選択図形を削除（直線削除時は関連クロソイドも連動削除）
- Undo（`Ctrl+Z`、最大 500 手順）
- ビュー操作: マウスホイールズーム・左/中ドラッグでパン・`Ctrl+0` で全体表示

#### 右パネル（第5章）
- マウスカーソルのワールド座標リアルタイム表示（小数点以下3桁）
- 図形選択コンボボックス（隣接図形を優先表示、`[順]`/`[逆]` で接続方向を表示）
- 道なり選択: 高優先候補が1件の場合にコンボを連鎖的に自動補完
- 直線・線分・円・円弧のプロパティ表示・数値入力編集
- マウスホイールによるスピンボックスの精密編集（変更は Undo に記録）
- 子の図形リスト表示（直線→線分一覧、円→円弧一覧）
- 円弧を追加 / 全追加ボタン（空き区間がある場合）
- 直線・線分の始点/終点 Copy & Paste ボタン
- 図形ホバー時のニックネーム・タイプ・親情報表示
- 縦断設計情報の表示（勾配直線・縦断曲線の一覧）
- 図形の削除ボタン（確認ダイアログ付き）
- 図形の再描画ボタン
- ニックネーム管理: 各図形に任意のニックネームを設定・保存

#### 縦断線形設計（第6章）
- 縦断線形設計ウィンドウ（`Ctrl+Shift+V`）
- 勾配直線モード（`G`）: クリックで始点→終点を指定して勾配直線を追加
- 勾配直線の重複区間自動置換・隣接スナップ
- 縦断曲線（放物線）の挿入（PVI・VPC・VPT・K 値の自動計算）
- カラーバー表示: 平面線形要素を種別ごとに色分け（線分=青・クロソイド=緑・円弧=紫）
- Undo（`Ctrl+Z`、最大 50 手順）/ Redo（`Ctrl+Y`・`Ctrl+Shift+Z`）

#### 3D走行ビューア（第7章）
- `Ctrl+Shift+3` で Panda3D による 3D ビューアを別プロセス起動
- 走行対象: 選択した平面線形要素チェーン（未選択なら全要素）
- 縦断線形（勾配直線・縦断曲線）に基づく高さ補間
- 道路幅 3.5m の路面メッシュ・センターライン・路肩白線・橋脚（約30m おき）
- 路面表示 ON/OFF（`R`）
- 視点モード: 追従視点・車載視点（`V`）・俯瞰視点・固定俯瞰（`O`）
- 複数台走行（トラフィック）: `P`/`Shift+P` で周囲車両を追加・削除
- オートドライブ（`A`）: 走行履歴スタック（最大10要素）付き、`←`/`→` で履歴移動
- 速度変更（`↑`/`↓`）・一時停止（`Space`）・100m 前後移動（`←`/`→`）

#### ファイル操作
- `.rdjson`（JSON）形式での保存・読み込み
- 起動時ファイル指定オプション（`-i FILE`）
- 保存前の ID 重複自動検出・修正
- ロード時の ID 衝突自動修正（クロソイド参照を維持）
- 旧フォーマットとの後方互換

#### テスト・品質
- テスト 1,275件、カバレッジ 93%（CI: ubuntu/windows/macos × Python 3.11/3.12）
- 要求仕様書 第4〜6章の GUI 適合確認テスト 64件（`-m spec`、手動実行）
- Codecov によるカバレッジレポート

### Fixed

- `VerticalAlignmentWindow`: `Ctrl+Y` / `Ctrl+Shift+Z` で Redo が動作しないバグ（`keyPressEvent` の判定順序の誤り）
- `road_viewer`: `_add_traffic_car` の負オフセットループで `NameError` が発生するバグ
- `road_viewer`: オートドライブ中の `_ad_advance` で `NameError` が発生するバグ
- クロソイドの snap デフォルトが `on` になっていたバグ（正: `off`）
- 保存した `.rdjson` を再ロードするとクロソイドが消えるバグ
- 円弧削除時に線分が増殖するバグ
- 線分と snap したとき線分が削除されないバグ（2件）
- 右パネルのプロパティ変更が Undo に2回積まれるバグ
- コンボボックスが埋まったときに次のボックスの高優先項目が更新されないバグ
- オフセット拘束の計算バグ
- シリアライズの実装漏れ（テストで検出）

---

[1.1.0]: https://github.com/vert2air/road-designer/releases/tag/v1.1.0
[1.0.0]: https://github.com/vert2air/road-designer/releases/tag/v1.0.0
